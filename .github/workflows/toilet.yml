name: Enable RDP with Pinggy

on:
  workflow_dispatch:

jobs:
  enable-rdp:
    runs-on: windows-latest
    steps:
      - name: 🖥️ Cài noVNC và chạy
        run: |
          git clone https://github.com/novnc/noVNC tools/novnc
          pip install websockify
          Start-Process -NoNewWindow -FilePath "python" -ArgumentList "-m", "websockify", "6080", "localhost:5900"
      - name: ✅ Done
        run: echo skibidi
